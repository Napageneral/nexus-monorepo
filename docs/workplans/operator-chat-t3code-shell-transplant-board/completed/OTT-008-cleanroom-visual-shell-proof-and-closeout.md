# OTT-008 Cleanroom Visual Shell Proof And Closeout

## Goal

Produce a cleanroom-backed recorded proof that the global `Chat` tab now uses
the real transplanted upstream-derived shell end to end.

## Why

The value of this board is not only correctness.
It is visual and interaction fidelity to the intended product direction.

## Scope

- update the operator-chat validation ladder for the transplanted shell
- extend browser proof coverage for the preserved sidebar grammar, lane
  workspace shell, and lane actions
- capture one Dispatch-standard whole-session recorded cleanroom bundle for the
  transplanted shell
- close the board with truthful proof artifacts and summary docs

## Implementation Notes

- the proof should show the global `Chat` tab inside the console host
- the proof should visibly demonstrate the preserved upstream sidebar grammar
- the proof should include agent-group selection, worker-lane chat, lane-action
  invocation, approvals, and linked public conversation context

## Acceptance

- the canonical proof bundle clearly shows the transplanted upstream-derived
  shell
- the validation ladder points at the live proof entrypoints and artifact
  bundle
- the board can be closed with truthful review evidence

## Validation

- dedicated cleanroom run
- promoted recorded artifact bundle

## Current Result

- the dedicated cleanroom run now passes through the canonical recorded capture
  wrapper at
  `/Users/tyler/nexus/home/projects/nexus/nex/scripts/e2e/operator-chat-cleanroom-capture.sh`
- the canonical recorded bundle now exists at
  `/Users/tyler/nexus/state/artifacts/validation/cleanroom/operator-chat-cleanroom/20260407T195221Z`
- that bundle retains the Dispatch-standard primary recording at
  `videos/full-session.webm` and the shared recording manifest at
  `review/full-session-recording.json`
- the proof now visibly demonstrates the transplanted shell through preserved
  sidebar grammar, lane-action creation and invocation, manager chat, worker
  direct chat, approval resolution, delivery switching, and replay recovery

## Closeout

- this board is complete because the transplanted upstream-derived shell now
  has one truthful recorded cleanroom proof bundle proving the end-to-end
  operator journey from the global `Chat` tab
