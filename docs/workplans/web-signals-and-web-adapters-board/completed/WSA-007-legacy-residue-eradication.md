# WSA-007 - Legacy Residue Eradication

## Outcome

Completed for supported runtime surfaces.

Removed:

- old `packages/apps/website-input` root
- old `packages/website-input` root
- active script and runbook references to the old hybrid package family
- active package-local split-brain storage inside `web-signals`

Remaining references are historical analysis or one-time attribution DB
migration context, not supported runtime or operator surfaces.
