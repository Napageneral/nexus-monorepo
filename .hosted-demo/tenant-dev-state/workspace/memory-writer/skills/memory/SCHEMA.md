# Memory System Schema (Split Ledgers)

Canonical runtime schema lives in Nex TS:
- `src/db/memory.ts` (`memory.db`)
- `src/db/embeddings.ts` (`embeddings.db`)
- `src/db/identity.ts` (entities in `identity.db`)