# Web Signals Package Docs

These docs define the operator-facing contract for the hard-cut `web-signals`
package family.

The package is organized around two practical lanes:

- installation and token lifecycle
- authenticated collector routing and records-backed QA inspection

Package-local docs should stay focused on how operators install, rotate, and
inspect `web_installation_id` records.
