# Upstream apps/web Baseline

This directory is a clean local copy of upstream `t3code/apps/web`.

Purpose:

- provide a non-destructive upstream fork baseline for the Nex operator-chat
  reset
- anchor the reset against a real upstream tree rather than the current custom
  derivative app

Current upstream source:

- `/Users/tyler/nexus/home/projects/t3code/apps/web`
- commit `28e481eb`

This directory should remain as close to upstream as possible while the reset
board is active.
