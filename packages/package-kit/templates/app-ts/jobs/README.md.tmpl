# Jobs

Place package-local job handlers and job definition seed helpers here.

The shared scaffold includes this directory so headless workflow or job-pack
apps do not need to invent their package layout later.
