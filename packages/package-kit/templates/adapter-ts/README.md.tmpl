# {{DISPLAY_NAME}}

This adapter package was scaffolded by `nex package init`.

Primary capability declarations live in:

- `adapter.nexus.json` for package metadata and entrypoint wiring
- `api/openapi.yaml` for the package capability contract

Primary commands:

- `nex package validate .`
- `nex package release .`
- `nex package smoke . --frontdoor-origin <url> --frontdoor-api-token <token> --server-id <id>`
