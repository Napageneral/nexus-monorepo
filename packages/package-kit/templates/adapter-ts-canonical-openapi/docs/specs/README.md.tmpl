# Specs

Capture the full provider surface and the projection contract decisions here.

Document:

- which upstream resources and operations are in scope for the package
- how provider-native operation ids map into the method catalog
- which projection families, routing rules, backfill strategy, and monitor
  strategy the package owns
