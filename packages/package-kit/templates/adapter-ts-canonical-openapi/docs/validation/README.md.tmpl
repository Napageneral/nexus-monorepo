# Validation

Use this ladder to prove provider truth and projection truth.

Validation should answer:

- does auth bind correctly for the provider account
- does `adapter.health` reflect the real upstream state
- do representative provider-native reads and writes match provider behavior
- does the projection contract describe and prove the intended ingest behavior

Do not claim projection behavior here unless the package actually implements it.
