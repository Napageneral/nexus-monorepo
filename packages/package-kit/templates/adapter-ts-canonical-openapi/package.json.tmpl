{
  "name": "{{PACKAGE_ID}}",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/materialize-openapi.mjs && rm -rf dist && pnpm exec esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js --banner:js='#!/usr/bin/env node'",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nexus-project/adapter-sdk-ts": "file:../nexus-adapter-sdks/nexus-adapter-sdk-ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0",
    "yaml": "^2.8.1"
  }
}
