# GitLab Adapter Workplan

The active execution lane for GitLab no longer lives in this package-local
directory.

Use the Nex workplans for:

- forge provider family convergence
- deferred GitLab provider proof once credentials exist
- app migration away from the legacy unified `git` package
