# Bitbucket Adapter Workplan

The active execution lane for Bitbucket no longer lives in this package-local
directory.

Use the Nex workplans for:

- forge provider family convergence
- app migration away from the legacy unified `git` package
- agent-use proof through the mounted capability tree
