{
  "name": "@nexus-project/mailchimp",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "description": "Read-only Mailchimp Marketing and Transactional evidence adapter for Nex",
  "main": "dist/index.js",
  "bin": {
    "mailchimp-adapter": "dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && pnpm exec esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js --banner:js='#!/usr/bin/env node' && chmod +x dist/index.js",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nexus-project/adapter-sdk-ts": "file:../nexus-adapter-sdks/nexus-adapter-sdk-ts",
    "csv-parse": "^6.1.0",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/node": "24.10.13",
    "esbuild": "0.27.3",
    "typescript": "5.9.3",
    "vitest": "4.0.18"
  }
}
