{
  "name": "@nexus-project/website-input-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared collector contract and runtime-envelope mapping for website input events.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nexus-project/website-input-core": "file:../core"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^4.0.5"
  }
}
