# API

This directory is the canonical home for platform-owned OpenAPI contracts.

Owned here:

- `frontdoor/` - Frontdoor platform API
- `nex/` - Nex runtime API

Package-owned APIs do not live here.
They live in package-local `api/` directories under `packages/`.
