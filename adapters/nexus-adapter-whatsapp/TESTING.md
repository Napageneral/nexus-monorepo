# WhatsApp Testing

1. `pnpm test`
2. `pnpm build`
3. `./scripts/package-release.sh`

Green bar:

- archive exists under `dist/`
- archive contains `adapter.nexus.json`
- archive contains `bin/nexus-adapter-whatsapp`
- archive contains `dist/`
- archive contains `node_modules/`
