# PatientNow EMR Adapter Package Install Workplan

Completed local slice:

1. add `adapter.nexus.json`
2. add package release script
3. package the adapter as a runtime-installable tarball
4. validate operator install and runtime restart rehydration
