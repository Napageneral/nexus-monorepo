# Nex API Contract

This directory is reserved for the canonical generated OpenAPI artifact for the Nex runtime API.

Per the current rollout plan, `nex/openapi.yaml` should be generated after the transport-surface hard cut lands.
