import { NexusEvent } from "./protocol.js";
import type { AdapterContext, AdapterDefinition } from "./run.js";
export type EmitFunc = (event: NexusEvent) => void;
export type PollConfig = {
    intervalMs: number;
    fetch: (ctx: AdapterContext, since: Date, account: string) => Promise<{
        events: NexusEvent[];
        newCursor?: Date;
    }> | {
        events: NexusEvent[];
        newCursor?: Date;
    };
    initialCursor?: Date;
    errorBackoffMs?: number;
    maxConsecutiveErrors?: number;
};
export type MonitorHandler = NonNullable<AdapterDefinition["monitor"]>;
export declare function pollMonitor(config: PollConfig): MonitorHandler;
